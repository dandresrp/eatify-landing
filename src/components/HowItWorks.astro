---
// How It Works Section - Eatify Landing Page
const steps = [
  {
    number: "01",
    icon: "📲",
    title: "Descarga y crea tu cuenta",
    description:
      "Descarga Eatify gratis desde App Store o Google Play. Regístrate en segundos y comienza tu prueba Premium de 30 días sin costo.",
    highlight: "Gratis en iOS y Android",
  },
  {
    number: "02",
    icon: "🔍",
    title: "Escanea tus productos",
    description:
      "Usa tu cámara para escanear códigos de barras o fotografiar tus tickets de compra. Eatify reconoce automáticamente los productos y sus fechas de vencimiento.",
    highlight: "Reconocimiento automático",
  },
  {
    number: "03",
    icon: "✨",
    title: "Deja que Eatify trabaje por ti",
    description:
      "Recibe alertas antes de vencimientos, descubre recetas con tus ingredientes disponibles y obtén planes nutricionales personalizados. ¡Así de simple!",
    highlight: "Gestión 100% automática",
  },
];
---

<section id="how-it-works" class="py-20 bg-white">
  <div class="container mx-auto px-4 sm:px-6 lg:px-8">
    <!-- Section Header -->
    <div class="text-center max-w-3xl mx-auto mb-16">
      <h2 class="text-3xl sm:text-4xl lg:text-5xl font-bold text-gray-900 mb-4">
        Comienza en
        <span class="text-emerald-600">3 pasos simples</span>
      </h2>
      <p class="text-lg text-gray-600">
        No necesitas ser experto en tecnología. Eatify está diseñado para ser
        intuitivo y fácil de usar desde el primer día
      </p>
    </div>

    <!-- Steps -->
    <div class="relative max-w-5xl mx-auto">
      <!-- Connection Line (hidden on mobile) -->
      <div
        class="hidden lg:block absolute top-24 left-0 right-0 h-1 bg-gradient-to-r from-emerald-200 via-emerald-400 to-emerald-600"
        style="z-index: 0;"
      >
      </div>

      <div class="grid md:grid-cols-3 gap-8 relative" style="z-index: 1;">
        {
          steps.map((step, index) => (
            <div class="relative">
              {/* Step Card */}
              <div class="bg-white rounded-2xl p-8 border-2 border-gray-200 hover:border-emerald-400 transition-all duration-300 hover:shadow-xl h-full">
                {/* Step Number */}
                <div class="flex items-center justify-between mb-6">
                  <span class="text-6xl font-bold text-emerald-100">
                    {step.number}
                  </span>
                  <div class="w-16 h-16 rounded-xl bg-gradient-to-br from-emerald-400 to-lime-500 flex items-center justify-center text-3xl shadow-lg">
                    {step.icon}
                  </div>
                </div>

                {/* Content */}
                <h3 class="text-xl font-bold text-gray-900 mb-3">
                  {step.title}
                </h3>

                <p class="text-gray-600 mb-4 leading-relaxed">
                  {step.description}
                </p>

                {/* Highlight */}
                <div class="inline-flex items-center gap-2 px-3 py-2 bg-emerald-50 rounded-lg border border-emerald-200">
                  <svg
                    class="w-4 h-4 text-emerald-600"
                    fill="currentColor"
                    viewBox="0 0 20 20"
                  >
                    <path
                      fill-rule="evenodd"
                      d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z"
                      clip-rule="evenodd"
                    />
                  </svg>
                  <span class="text-sm font-semibold text-emerald-700">
                    {step.highlight}
                  </span>
                </div>
              </div>

              {/* Arrow (hidden on mobile and last item) */}
              {index < steps.length - 1 && (
                <div class="hidden md:block absolute top-1/2 -right-4 transform -translate-y-1/2 text-emerald-400 text-2xl z-10">
                  →
                </div>
              )}
            </div>
          ))
        }
      </div>
    </div>

    <!-- Bottom CTA Section -->
    <div class="mt-16 text-center">
      <div
        class="max-w-2xl mx-auto bg-gradient-to-r from-emerald-500 to-lime-500 rounded-3xl p-8 sm:p-12 text-white shadow-2xl"
      >
        <h3 class="text-2xl sm:text-3xl font-bold mb-4">
          ¿Listo para transformar tu alimentación?
        </h3>
        <p class="text-lg mb-6 text-emerald-50">
          Únete a la revolución de la gestión alimentaria inteligente
        </p>

        <div class="flex flex-col sm:flex-row gap-4 justify-center">
          <a
            href="#download"
            class="inline-flex items-center justify-center gap-2 px-8 py-4 bg-white text-emerald-600 rounded-xl font-semibold hover:bg-gray-50 transition-all hover:scale-105 shadow-lg"
          >
            <svg
              class="w-5 h-5"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M12 18h.01M8 21h8a2 2 0 002-2V5a2 2 0 00-2-2H8a2 2 0 00-2 2v14a2 2 0 002 2z"
              ></path>
            </svg>
            Descargar ahora gratis
          </a>

          <a
            href="#pricing"
            class="inline-flex items-center justify-center gap-2 px-8 py-4 bg-emerald-700 text-white rounded-xl font-semibold hover:bg-emerald-800 transition-all hover:scale-105 border-2 border-white/20"
          >
            Ver planes y precios
            <svg
              class="w-5 h-5"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M17 8l4 4m0 0l-4 4m4-4H3"></path>
            </svg>
          </a>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .grid > div:hover {
    transform: translateY(-4px);
  }
</style>
